{
  "name": "pds",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:worker": "worker-build --release .",
    "test:rust": "cargo test",
    "test": "cargo test"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
